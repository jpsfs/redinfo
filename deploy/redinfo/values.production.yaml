namespace: production

replicaCount: 1

image:
  backend:
    repository: jpsfs/redinfo-backend
    tag: "production"
    pullPolicy: IfNotPresent
  frontend:
    repository: jpsfs/redinfo-frontend
    tag: "production"
    pullPolicy: IfNotPresent

service:
  backend:
    type: ClusterIP
    port: 3000
  frontend:
    type: ClusterIP
    port: 80

ingress:
  enabled: true
  className: public
  hosts:
    - host: app.cvpcampo.org
      paths:
        - path: /
          service: frontend
          servicePort: 80
  # TODO: production needs real TLS (cert-manager + ClusterIssuer), not HTTP-only —
  # OAuth login will not work without it. See deploy/redinfo/README.md.
  tls: []

resources:
  backend:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 512Mi
  frontend:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 150m
      memory: 128Mi

env:
  backend:
    NODE_ENV: "production"
    PORT: "3000"
    ATTACHMENTS_DIR: "/app/uploads"
    FRONTEND_URL: "https://app.cvpcampo.org"
  frontend: {}

# Populated by the pipeline from a `redinfo-production` ADO variable group via
# --set-string; left empty here so nothing sensitive is ever committed.
secrets: {}

uploads:
  enabled: true
  size: 20Gi
  mountPath: /app/uploads

# TODO: decide whether production seeds an initial admin the same way staging does,
# or that account is created out-of-band before go-live.
seed:
  enabled: false

postgresql:
  enabled: true
  auth:
    username: redinfo
    database: redinfo_production
    password: "changeme"
    postgresPassword: "changeme"
  primary:
    persistence:
      enabled: true
      size: 20Gi
