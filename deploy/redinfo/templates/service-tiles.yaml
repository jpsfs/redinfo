{{- if .Values.basemap.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "redinfo.fullname" . }}-tiles
  labels:
    app.kubernetes.io/name: {{ include "redinfo.name" . }}
    app.kubernetes.io/component: tiles
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "redinfo.name" . }}
    app.kubernetes.io/component: tiles
{{- end -}}
