Thank you for installing redinfo ({{ .Release.Name }} in namespace {{ .Release.Namespace }}).

To verify the rendered resources locally:

  helm template {{ .Release.Name }} deploy/redinfo --values deploy/redinfo/values.staging.yaml

{{- if .Values.ingress.enabled }}
{{- range .Values.ingress.hosts }}

Once the ingress controller has picked this up, the app should be reachable at:
  http://{{ .host }}
{{- end }}
{{- end }}
